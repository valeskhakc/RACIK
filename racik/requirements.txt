ortools>=9.10
fastapi>=0.110
uvicorn>=0.27
rapidfuzz>=3.6
pandas>=2.0
openpyxl>=3.1
pytest>=8.0
httpx>=0.27          # required by fastapi.testclient.TestClient (tests only)
python-dotenv>=1.0   # loads racik/.env — see config.py

# Optional: only for the Amazon Bedrock providers (BedrockConverseClient in
# bedrock_converse.py, BedrockSeaLionClient in bedrock.py)
# boto3>=1.34
